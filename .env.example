# AI Backend (Bedrock preferred, falls back to Anthropic API key)
AWS_PROFILE=nurena-bedrock-account
AWS_REGION=us-west-2

# Anthropic API key (optional - only needed if not using Bedrock)
ANTHROPIC_API_KEY=
